/**
 * Stores the data information for a skill badge
 * @param {string} imageURL - URL of image representing the skill
 * @param {string} name - The name of the skill
 */
interface SkillData {
    imageURL?: string,
    name?: string,
    key: number,
};